
var expect = require("expect.js")
,   pth = require("path")
,   fs = require("fs")
;
describe("XXX", function () {
    // XXX
    //  get a port
    //  start a server instance using a given configuration
    //      - store is in tmp, make sure it gets cleaned up
    //      - use that port
    //      - use localhost
    //  deploy
    //      - a git app
    //      - a local app
    //      - a local static (and check that it doesn't copy)
    //  start
    //  stop
    //  remove (all)
    
    it("XXX", function () {
        
    });
});
